#!/usr/bin/python3
def max_integer(my_list=[]):
    max = 0
    if my_list == []:
        return (None)
    for a in range(len(my_list)):
        if (a = 0):
            max = my_list[a]
        elif (my_list[a] > max):
            max = my_list[a]
    return(max)
